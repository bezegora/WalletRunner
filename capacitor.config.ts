import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.walletrunner.app',
    appName: 'WalletRunner',
    webDir: 'dist/wallet-runner',
    bundledWebRuntime: false
};

export default config;
